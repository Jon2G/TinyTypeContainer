name: CI

on:
  push:
    branches:
      - main
      - release/*
  pull_request:
    branches:
      - main
      - release/*

jobs:
  build:
    env:
      BUILD_CONFIG: "Release"
      SOLUTION: "TinyTypeContainer.sln"
      PROJECT: "TinyTypeContainer.csproj"
      TEST_PROJECT: "../Test/Test.csproj"

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src

    steps:
      - uses: actions/checkout@v2

      - name: Check branch
        run: |
          echo commit message: '${{ github.event.head_commit.message }}'
          echo Branch: '${{ github.ref }}'
          echo Branch: '${{ github.event_name }}'

      - name: Check merge commit
        if: startsWith(github.ref, 'refs/heads/main') && (!contains(github.event.head_commit.message, 'Merge pull request ') || !contains(github.event.head_commit.message,'release/'))
        run: |
          echo INVALID COMMIT RELEASE MESSAGE: '${{ github.event.head_commit.message }}'
          exit 1

      - uses: suisei-cn/actions-download-file@818d6b7dc8fe73f2f924b6241f2b1134ca1377d9 # 1.6.0
        if: startsWith(github.ref, 'refs/heads/main') && contains(github.event.head_commit.message, 'Merge pull request ') && contains(github.event.head_commit.message,'release/')
        id: GetBuildVersion # Remember to give an ID if you need the output filename
        name: Download GetBuildVersion.psm1
        with:
          url: "https://raw.githubusercontent.com/MADE-Apps/MADE.NET/refs/heads/main/build/GetBuildVersion.psm1"
          target: src/
          filename: GetBuildVersion.psm1

      - name: GetBuildVersion.psm1
        if: startsWith(github.ref, 'refs/heads/main') && contains(github.event.head_commit.message, 'Merge pull request ') && contains(github.event.head_commit.message,'release/')
        run: |
          SET HEAD_COMMIT = '${{github.event.head_commit.message}}'
          Import-Module .\GetBuildVersion.psm1
          Write-Host $Env:HEAD_COMMIT
          $version = GetBuildVersion -VersionString $Env:HEAD_COMMIT
          echo "BUILD_VERSION=$version" | Out-File -FilePath $Env:HEAD_COMMIT -Encoding utf-8 -Append
          echo $version
        shell: pwsh

      - name: Setup NuGet
        if: startsWith(github.ref, 'refs/heads/main') && contains(github.event.head_commit.message, 'Merge pull request ') && contains(github.event.head_commit.message,'release/')
        uses: NuGet/setup-nuget@v2

      - name: Setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            2.0.x
            2.1.x
            6.0.x
            7.0.x
            8.0.x
            9.0.x

      - name: Restore dependencies
        run: dotnet restore $SOLUTION

      - name: Build project
        run: dotnet build $PROJECT --configuration Release

      - name: Build solution
        run: dotnet build $SOLUTION --configuration $BUILD_CONFIG --no-restore

      - name: Run unit tests with report
        run: dotnet test $TEST_PROJECT --configuration Release --no-build --logger "trx;LogFileName=./../../src/TestResults.trx"

      - name: Show files
        run: ls -la

      # - name: Upload test results
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: TestResults
      #     path: ./TestResults.trx

      - name: Publish
        if: startsWith(github.ref, 'refs/heads/main') && contains(github.event.head_commit.message, 'Merge pull request ') && contains(github.event.head_commit.message,'release/')
        run: dotnet nuget push **\*.nupkg -Source 'https://api.nuget.org/v3/index.json' -ApiKey ${{secrets.NUGET_API_KEY}}
